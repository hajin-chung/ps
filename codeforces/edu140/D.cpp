#include <stdio.h>

int n;
char s[20];
int a[20];
int sm, bg;

void rec(int lev, int isleft) {
  if
}

int main() {
  int i;
  scanf("%d", &n);
  scanf("%s", &s[1]);
  for(i=1;i<=n;i++) a[i]=s[i]-'0';
  rec(n, 1);
  for(i=sm+1;i<bg;i++) printf("%d ", i);
  printf("\n");
}